create database kzs;
create database  if not exists kzs;
use kzs;
-- Create table student(
--  id int primary key,
--  name varchar(100),
--  age int not null
-- );
-- -- insert into student values(1,"Sajidahamed",23);
-- -- insert into student values(2,"Meem",22);
-- -- insert into student values (3,"Faiz",2);
-- -- select * from student

CREATE TABLE students(
rollno int primary key,
name varchar(50)
);